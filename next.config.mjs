/** @type {import('next').NextConfig} */
const nextConfig = {
   // Remove or comment out any redirects related to Google callback
   // async redirects() {
   //    return [
   //       {
   //          source: '/api/google-calendar/callback',
   //          destination: '/api/google-callback',
   //          permanent: true,
   //       },
   //    ]
   // },
};

export default nextConfig;